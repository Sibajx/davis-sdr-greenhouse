This folder is for raw hexadecimal payloads or unprocessed RF frame
captures (e.g. samples used to validate the CRC-16 and the bit-level
decoder). Currently no files are included here; a small sample of example
frames could be added if you'd like to accompany the repository with test
data for the decoder.
