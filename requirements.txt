# davis_monitor.py uses only the Python 3.9+ standard library:
#   subprocess, curses, threading, time, os, re, csv, sys, signal, select,
#   struct, logging, enum, dataclasses, collections, datetime, typing
#
# There are no external pip dependencies.
#
# External dependency NOT installable via pip:
#   rtldavis  -> third-party binary that decodes the RF telemetry from the
#                Davis Vantage Pro 2 station. Must be compiled/installed
#                separately and be available on the system PATH.
#                Reference: https://github.com/rtldavis
